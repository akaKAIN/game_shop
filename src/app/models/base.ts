export interface SimpleContactItem {
  contactTitle: string;
  contactText: string;
  contactIconType: string;
  contactLink?: string;
}

export interface NavMenuOption {
  text: string;
  link: string;
}
